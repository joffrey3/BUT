function makeServiceAjax(){
	let urlBase = "https://www.omdbapi.com/"
	let apiKey = "2fcb2848"

	let service = {
		doRequest
	};

	function doRequest(search){
		// A completer
		// fait une requete ajax et renvoie 
		// une promesse
	}
	return service
}
